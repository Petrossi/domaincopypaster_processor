spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/domainsurvey_test?useUnicode=yes&characterEncoding=UTF-8&autoReconnect=true
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        format_sql: true
#        show_sql: true
logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql: trace